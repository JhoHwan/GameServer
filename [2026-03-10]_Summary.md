# 2026-03-10 작업 요약

## 1. 비동기 Job 실행 안정성 강화 및 크래시 방지
- `Field` 클래스의 모든 비동기 작업(`DoAsync`)에서 `this` 포인터를 직접 캡처하던 방식을 `self = shared_from_this()`를 사용하는 방식으로 개선하여, 비동기 실행 시점에 객체가 파괴되어 발생하는 댕글링 포인터 참조(DEADLYSIGNAL) 오류를 원천 차단했습니다.
- `GameSession::OnDisconnected()` 함수에 플레이어 및 필드 존재 여부를 확인하는 널 포인터 체크 방어 코드를 추가하여, 맵 로딩 중 접속 종료 시 발생하던 예외 상황을 해결했습니다.

## 2. 필드 시스템의 데이터 중심(Data-Driven) 설계 및 리팩토링
- `nlohmann/json` 라이브러리를 도입하여 맵 설정 정보를 외부 JSON 파일(`Test_Map_0.json`)에서 관리하도록 구조를 개선했습니다.
- `FieldData` 및 `FieldManager` 클래스를 고도화하여 네비게이션 메시와 스폰 포인트 정보를 데이터 파일로부터 동적으로 로드하고 관리할 수 있게 되었습니다.
- 필드 내 유저가 모두 나갔을 때 10초 뒤 자동 삭제되는 로직에 `DestroyToken`을 도입하여, 삭제 예약 시간 내 유저가 재입장할 경우 파괴 작업이 안전하게 취소되도록 로직을 보정했습니다.

## 3. 리소스 관리 자동화 및 빌드 환경 개선
- CMake 설정에 `CopyResources` 커스텀 타겟을 추가하여, 빌드 시 `Resources` 폴더 내의 JSON 설정과 네비게이션 메시 파일들이 바이너리 실행 경로로 자동 복사되도록 자동화했습니다.
- `SC_START_FIELD_LOADING` 패킷 구조에서 불필요한 필드를 제거하고 로직을 단순화하여 네트워크 대역폭 효율을 높였습니다.

## 4. 데이터 타입 정밀도 조정 및 좌표계 변환 로직 정교화
- `Vector3` 및 `yaw` 데이터 타입을 `double`에서 `float`로 변경하여 데이터 크기를 최적화하고 연산 효율을 높였습니다.
- 언리얼 엔진 좌표계(cm)와 Detour 네비메시 좌표계(m) 간의 변환 시 발생하는 축 매핑 및 스케일 오차를 수정하여 정밀한 길찾기가 가능하도록 개선했습니다.


[코드 변경 사항 요약]
```cpp
// 1. 비동기 실행 시 생명 주기 보장을 위한 shared_from_this() 활용 (Field.cpp)
void Field::LeavePlayer(shared_ptr<PlayerCharacter> player, shared_ptr<Field> nextField)
{
    // self를 값으로 캡처하여 비동기 작업이 끝날 때까지 Field 객체의 생존을 보장함
    DoAsync([self = shared_from_this(), player = std::move(player), nextField = std::move(nextField)]() {
        player->SetField(nullptr);
        self->_players.erase(player);

        if(self->_players.empty())
        {
            // DestroyToken을 캡처하여 10초 내 재입장 시 파괴 로직을 무효화함
            JobRef job = make_shared<Job>([self, fieldId = self->_id, Token = self->DestroyToken.load()]()
            {
                if(self->DestroyToken.load() != Token) return; 
                GFieldManager.Destroy(fieldId);
            });
            LJobTimer.Reserve(10000, self->GetJobQueue(), job);
        }
    });
}

// 2. 널 포인터 체크를 통한 예외 상황 방어 (GameSession.cpp)
void GameSession::OnDisconnected()
{
    PacketSession::OnDisconnected();
    if(_playerRef)
    {
        // 플레이어가 필드에 진입하기 전(로딩 중)에 끊기는 경우를 대비하여 체크
        auto field = _playerRef->GetField();
        if(field) field->LeavePlayer(_playerRef);
    }
    _playerRef.reset();
}

// 3. 외부 데이터 기반의 동적 좌표 세팅 (Field.cpp)
void Field::EnterPlayer(weak_ptr<PlayerCharacter> player)
{
    DestroyToken.fetch_add(1); // 입장 시 토큰을 올려 파괴 예약을 무효화
    DoAsync([self = shared_from_this(), playerWeak = std::move(player)]()
    {
        auto player = playerWeak.lock();
        if(!player) return;

        self->_players.insert(player);
        // JSON 데이터에서 로드된 스폰 포인트 좌표를 적용 (하드코딩 제거)
        player->Transform()->SetPos(self->_fieldData->PlayerStarts()[0]);
        player->SetField(self);
        // ... 생략 ...
    });
}
```
