# 2026-03-12 작업 요약

## 1. 64비트 필드 ID 시스템 및 비트 연산 확립
- `Field` 객체의 고유 식별을 위해 상위 16비트를 `MapID`, 하위 48비트를 `InstanceID`로 사용하는 ID 체계를 구축했습니다.
- `Field::GetMapID()`, `Field::GetInstanceID()` 함수를 통해 비트 마스킹 연산으로 정보를 즉시 추출할 수 있도록 구현했습니다. (오류 수정 완료: `_id >> 48` 및 `0x0000FFFFFFFFFFFFULL` 마스크 적용)

## 2. 지능형 필드 매니저 및 자동 확장(Auto-scaling) 로직
- **이중 인덱싱 관리:** `MapID`별 인스턴스 목록(`unordered_map<uint16, unordered_set<shared_ptr<Field>>>`)과 고유 ID 검색 맵(`_fieldIdInstanceMap`)을 동시에 운영하여 검색 효율을 극대화했습니다.
- **부하 분산 기반 입장:** `GetField(mapId)` 호출 시, 해당 맵의 기존 인스턴스 중 수용 인원(`MAX_PLAYERS`)이 남은 곳을 우선 반환하고, 모든 인스턴스가 꽉 찼을 경우 자동으로 새 인스턴스를 `Create` 하도록 설계했습니다.
- **자원 자동 정리:** 필드 내 플레이어가 0명이 되면 `_destroyToken`을 활용한 지연 `Job`을 통해 10초 후 인스턴스를 자동으로 파괴하여 서버 자원을 최적화했습니다.

## 3. 포탈 이동 시스템 설계 (내일 진행 예정)
- 단순 좌표 이동을 넘어 목적지 맵의 특정 포탈 번호까지 지정하는 정밀 이동 시스템을 설계했습니다.
- `PortalInfo { PortalId, TargetMapId, TargetPortalId, Position }` 구조의 데이터 기반 이동 방식을 채택했습니다.


[코드 변경 사항 요약]
```cpp
// 1. Field ID 추출 및 상태 관리 (Field.h)
uint16 GetMapID() const { return static_cast<uint16>(_id >> 48); }
uint64 GetInstanceID() const { return (_id & 0x0000FFFFFFFFFFFFULL); }
bool CanEnterField() const { return _currentPlayerCount.load() < MAX_PLAYERS; }

// 2. 부하 분산 기반 필드 획득 (FieldManager.cpp)
shared_ptr<Field> FieldManager::GetField(uint16 mapId) {
    READ_LOCK;
    auto it = _fields.find(mapId);
    if (it != _fields.end()) {
        for (const auto& f : it->second) {
            if (f->CanEnterField()) return f; // 여유 있는 인스턴스 반환
        }
    }
    return Create(mapId); // 꽉 찼거나 없으면 새 인스턴스 생성
}

// 3. 안전한 인스턴스 파괴 (FieldManager.cpp)
void FieldManager::Destroy(uint64 fieldId) {
    WRITE_LOCK;
    auto it = _fieldIdInstanceMap.find(fieldId);
    if (it == _fieldIdInstanceMap.end()) return;
    
    shared_ptr<Field> field = it->second;
    _fieldIdInstanceMap.erase(it);
    _fields[field->GetMapID()].erase(field); // 정확한 버킷에서 제거
}
```

---

## 📅 내일의 할 일 (2026-03-13)
1. **정밀 포탈 이동 시스템 구현**
   - `FieldData` JSON 파싱 로직 확장 (`TargetMapId`, `TargetPortalId` 추가).
   - `PlayerCharacter`에 `_pendingMapId`, `_pendingPortalId` 상태 관리 추가.
   - 클라이언트 로딩 패킷(`CS_FIELD_LOADING_COMPLETE`) 처리 로직 고도화.
2. **이동 시스템 최적화**
   - `dtNavMeshQuery::raycast`를 활용한 직선 이동(Fast-path) 체크.
   - `CS_REQUEST_MOVE` 빈도 조절(Throttling) 및 목적지 보간 최적화.
