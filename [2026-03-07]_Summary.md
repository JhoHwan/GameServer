# 오늘 작업 요약 (2026-03-08)

## 1. NavMesh 기반 비동기 길 찾기 연산 적용 및 최적화
- `FieldInstance` 내에 `PlayerRequestMove` 로직을 구현하여 클라이언트 이동 요청(Click) 시 Detour의 `dtNavMeshQuery`를 이용해 목적지까지의 경로(Waypoints)를 찾아내는 연산을 성공적으로 적용함.
- `JobQueue` 시스템 내에서 단일 틱으로 길 찾기를 수행했음에도 첫 연산 1.2ms, 이후 캐시 히트로 0.1ms대의 뛰어난 성능을 기록하여 별도의 Compute 스레드 분리 없이도 현재 필드 큐에서 안전하고 빠르게 처리됨을 증명함.
- 연산된 경로 배열을 언리얼 클라이언트가 보간 이동할 수 있도록 `SC_MOVE_PATH` 패킷에 담아 응답 및 주변 유저에게 브로드캐스트하도록 구현.

## 2. 언리얼 엔진 호환성을 위한 클라이언트 패킷 제너레이터 리팩토링
- 프로토버프 패킷 자동 생성 스크립트(`PacketGen.bat`)를 언리얼 엔진의 C++ 모듈 시스템(Public/Private 폴더 분리) 규칙에 완벽히 맞도록 대대적으로 수정.
- `protoc` 실행 옵션에 `dllexport_decl=GAMENET_API` 매크로를 삽입하여, 언리얼 프로젝트 빌드 시 생성된 패킷 클래스(`.pb.cc/h`)들이 외부 모듈(DLL)에서도 안전하게 참조(Export)될 수 있도록 구조를 정립함.
- 생성 후 헤더(`.h`) 파일은 `Public` 폴더로, 소스(`.cc`) 파일은 `Private` 폴더로 자동 분리/이동되도록 파이프라인 자동화 완료.

## 3. 통합 LogManager 시스템 도입 및 매크로 확장
- C++20 `std::format`을 활용하여 타입에 구애받지 않고 가독성 높은 `LOG_DEBUG`, `LOG_INFO`, `LOG_WARN`, `LOG_ERROR` 매크로를 구축함.
- `ConcurrentQueue`와 전용 스레드(`std::jthread`)를 활용한 락프리 기반의 비동기 파일/콘솔 로깅 시스템을 구현하여, 로깅으로 인한 게임 로직의 병목과 멈춤(Block) 현상을 제거함.
- Windows와 Linux의 `Session::Disconnect` 등 주요 네트워크 이벤트의 하드코딩된 출력을 모두 새 통합 로그 시스템으로 전환.

## 4. FieldManager 지연 로딩(Lazy Loading) 및 서버 초기화 안정성 개선
- 서버 구동 시 `main.cpp`에서 `LogManager`와 `FieldManager`의 초기화 실패 여부를 검증하고 오류 발생 시 즉각 종료하도록 Error Handling 강화.
- `FieldManager`를 리팩토링하여 서버 시작 시 네비게이션 맵(`Test.bin`)만 캐싱하고, 실제 `FieldInstance`는 유저가 입장(또는 접근)할 때 비로소 생성(Create)되는 동적 지연 로딩(Lazy Loading) 구조로 개편하여 메모리와 자원을 절약함.
- 게임 접속 및 이동 로직을 `GameManager` 싱글톤으로 위임하여 패킷 핸들러와의 책임을 깔끔하게 분리함.

## 5. JobQueue 캡슐화를 위한 AsyncActor 도입
- 객체가 상속받아 손쉽게 자신만의 락프리 큐를 가질 수 있도록, 내부에 `JobQueueRef`를 할당받아 관리하고 `DoAsync`를 제공하는 `AsyncActor` 래퍼(Wrapper) 패턴을 `ServerCore`에 도입함.

## 6. 서버 측 플레이어 이동 보간(Interpolation) 및 상태 동기화 구현
- 클라이언트 이동 경로 요청 후, 서버 내부적으로도 플레이어의 위치를 실시간으로 추적하기 위해 `PlayerCharacter::GetCurrentPosition()` 함수를 구현함.
- 이동 시작 시 각 Waypoint 도달 예정 시간을 `_moveArrivalTimes` 배열에 미리 계산해 두어 연산량을 최소화함. (매 틱마다 남은 거리를 계산하지 않고 현재 시간(`now`)의 비율(Ratio)에 따라 선형 보간 수행).
- 예상 도착 시간에 맞춰 `JobTimer`에 목적지 도착 및 상태 해제(`_isMoving = false`) 이벤트를 예약(Reserve)하는 방식을 채택하여 최적화 달성.
- 새로운 이동 명령 수신 시 `moveToken` 방식을 통해 과거의 취소된 타이머 이벤트가 무시되도록 방어적 코드를 작성함.

## 7. Field 단위 2Hz 주기적 위치 동기화(Tick) 시스템 구축
- 플레이어 시야 동기화 및 충돌 판정을 위해 서버에서도 주기적인 필드 업데이트가 필요하므로 `FieldInstance::UpdatePlayerPosition()` 루프를 도입.
- 초기화 시점에 필드 객체가 완전히 생성된 후(`Init()`) 백그라운드에서 `JobTimer`를 통해 500ms(2Hz)마다 자기 자신을 재귀적으로 예약하는 구조를 구축함.
- `thread_local`로 선언된 `LJobTimer` 특성으로 인해 메인 스레드에서 생성자 예약 시 워커 스레드가 타이머를 소비하지 못하는 동기화 이슈를 발견하고, 별도의 `Init()` 함수를 통해 워커 스레드의 컨텍스트로 진입한 후 예약을 수행하도록 리팩토링.
- `std::enable_shared_from_this` 사용 시 생성자 내에서 `weak_from_this()` 호출 시 발생하는 빈 제어 블록(Empty) 이슈를 해결.

---

### [코드 변경 사항 요약]

```cpp
// Server/ServerCore/Util/LogManager.h : std::format을 활용한 가변 인자 로깅 매크로
#define LOG_INFO(Format, ...) LogManager::Instance().WriteLog(ELogLevel::Info, std::format(Format __VA_OPT__(,) __VA_ARGS__));
#define LOG_DEBUG(Format, ...) LogManager::Instance().WriteLog(ELogLevel::Debug, std::format(Format __VA_OPT__(,) __VA_ARGS__));
```

```cpp
// Server/GameServer/Contents/Field.cpp : 락프리 기반 필드 내부 길 찾기 연산
void FieldInstance::PlayerRequestMove(weak_ptr<PlayerCharacter> player, const Protocol::Vector3& dest)
{
    // FieldInstance의 JobQueue 내에서 실행 (단일 스레드 보장, Lock 불필요)
    DoAsync([this, player = std::move(player), dest]()
    {
        auto playerRef = player.lock();
        if(!playerRef) return;

        dtReal startPos[3] {playerRef->Transform()->GetPos().x, playerRef->Transform()->GetPos().z, playerRef->Transform()->GetPos().y};
        dtReal endPos[3] {dest.x(), dest.z(), dest.y()};

        dtQueryResult result;
        FindPath(startPos, endPos, result); // Detour NavMesh 연산 수행

        Protocol::SC_MOVE_PATH pkt;
        pkt.set_object_id(playerRef->GetId());

        // 찾아낸 경로 노드(Waypoints)들을 패킷에 적재
        for (int i = 0; i < result.size(); i++)
        {
            auto* pos = result.getPos(i);
            auto* waypoint = pkt.add_waypoints();
            waypoint->set_x(pos[0]);
            waypoint->set_y(pos[2]); // Y, Z축 언리얼 스위즐링 복원
            waypoint->set_z(pos[1]);
        }
        
        // 주변 유저에게 이동 경로 브로드캐스트
        BroadCast(ServerPacketHandler::MakeSendBuffer(pkt));
    });
}
```

```bat
:: Server/PacketGenerator/PacketGen.bat : 언리얼 모듈 시스템(GAMENET_API) 맞춤형 패킷 생성
:: ...
SET PUBLIC_DIR=C:\UnrealProject\MP2\Source\GameNet\Public\Proto
SET PRIVATE_DIR=C:\UnrealProject\MP2\Source\GameNet\Private\Proto

:: dllexport_decl 매크로를 삽입하여 .cc와 .h 생성 후, 헤더만 Public으로 이동
"%PROTOC_PATH%" -I="%PROTO_DIR%" --cpp_out=dllexport_decl=GAMENET_API:"%PRIVATE_DIR%" "%%f"
move /Y "%PRIVATE_DIR%\*.h" "%PUBLIC_DIR%\" >nul
```

```cpp
// Server/GameServer/Contents/Player.cpp : 도착 시간 사전 계산 및 예약
void PlayerCharacter::SetMoveInfo(std::vector<Vector3> waypoints, uint64 startTime, float speed)
{
    // ... 구간별 도착 시간 누적 및 배열 저장 생략 ...
    
    // 최종 목적지 도착 시간에 Job 예약
    JobRef job = make_shared<Job>([weakGameObject = weak_from_this(), moveToken = _moveStartTime]()
    {
        auto gameObject = weakGameObject.lock();
        if (!gameObject) return;
        auto self = static_pointer_cast<PlayerCharacter>(gameObject);
        
        // 이동 중 덮어씌워진 예전 이동 명령 취소 방어 코드
        if(moveToken != self->_moveStartTime) return;

        if(self->_isMoving)
        {
            self->_isMoving = false;
            self->Transform()->SetPos(self->_moveWaypoints.back());
        }
    });

    LJobTimer.Reserve(accumulatedTime - _moveStartTime, GetJobQueue(), job);
}
```

```cpp
// Server/GameServer/Contents/Field.cpp : 워커 스레드 안전한 2Hz 틱 예약
void FieldInstance::Init()
{
    // thread_local JobTimer가 워커 스레드에서 등록/소비될 수 있도록
    // 생성자에서 분리하여 객체 생성(make_shared)이 완전히 끝난 후 실행
    JobRef job = make_shared<Job>([weakSelf = weak_from_this()]() {
        auto self = weakSelf.lock();
        if(self == nullptr) return;

        self->UpdatePlayerPosition();
    });

    LJobTimer.Reserve(500, GetJobQueue(), job);
}
```