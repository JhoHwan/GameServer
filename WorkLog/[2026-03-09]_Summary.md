# 2026-03-09 작업 요약

## 1. 서버 내 이동 간 보간(Interpolation) 및 Field 위치 동기화 구현
- 클라이언트로부터 이동 요청 수신 시, 웨이포인트별 도착 시간을 미리 계산하여 `_moveArrivalTimes` 배열에 캐싱.
- `JobTimer`를 활용하여 500ms(2Hz) 주기로 현재 이동 중인 모든 플레이어의 위치를 보간(Lerp)하여 서버 내부 Transform 좌표를 동기화.

## 2. 클라이언트-서버 시간 동기화(TIME_SYNC) 및 구조 개선
- 클라이언트와 서버 간의 Heartbeat 및 Ping 처리를 위해 `CS_TIME_SYNC`, `SC_TIME_SYNC` 패킷 구조 도입.
- 평면상의 거리만 검증하여 Z축(높이) 차이로 인한 튕김 현상을 방지하도록 `Vector3::Dist2D` 도입 및 적용.
- 네비게이션 메시 로드 실패 시, FieldManager 내에 빈 인스턴스가 남지 않도록 Destroy 처리 로직 개선.

## 3. Unreal Detour 의존성 제거 및 기본(Vanilla) Detour 도입
- 언리얼 엔진 전용 커스텀 데이터 타입(ex: 8바이트 `double` 좌표계)과 구조체(`dtQueryResult`)에 의존하던 로직을 제거.
- 오리지널 Detour 라이브러리를 포팅하고 메모리 할당자(`DT_ALLOC_PERM`) 및 해제 함수(`dtFree`)를 기본 사양으로 교체.
- 길찾기 반환 방식을 구조체 대신 표준 C++ `std::vector<Vector3>`로 변경하여 범용성 확보.

## 4. RecastDemo Multi-Tile 네비메시 로더 구현 및 좌표계 스케일 변환
- RecastDemo에서 생성한 다중 타일(Tile Mesh) 구조의 `.bin` 네비메시 파일을 안정적으로 로드하도록 `NavMeshLoader` 개선.
- 파일 헤더 검증부(Magic Number, Version)와 타일별 메모리 동적 할당 및 예외 처리(Error Logging) 추가.
- Unreal 좌표계(cm 단위)를 Detour 좌표계(m 단위)로 맞추기 위해 검색 전 100으로 나누고(`/ 100.0f`), 탐색 결과를 반환할 때 다시 100을 곱하는(`* 100.0f`) 스케일 및 축 보정 적용.


[코드 변경 사항 요약]
```cpp
// 1. 기본 Detour 규격에 맞춘 길찾기 결과 벡터 반환 및 좌표계(cm <-> m) 보정 (Field.cpp)
void FieldInstance::FindPath(const Vector3& startPos, const Vector3& endPos, OUT std::vector<Vector3>& pathResult)
{
	// 언리얼 좌표(cm)를 Detour 좌표(m)로 축소 및 변환
	float startPt[3] { (float)startPos.x / 100.0f, (float)startPos.y / 100.0f, (float)startPos.z / 100.0f };
	float endPt[3] = { (float)endPos.x / 100.0f, (float)endPos.y / 100.0f, (float)endPos.z / 100.0f };

	// ... Detour 네비메시 길찾기 로직(findNearestPoly, findPath, findStraightPath) 수행 ...

	if(straightPathCount > 0)
	{
		for (int i = 0; i < straightPathCount; ++i)
		{
			// Detour에서 찾은 StraightPath(X,Y,Z / m)를 다시 언리얼 좌표계(X,Z,Y / cm)로 확장하여 반환
			pathResult.push_back(Vector3(straightPath[i*3] * 100.0f, straightPath[i*3+2] * 100.0f, straightPath[i*3+1] * 100.0f));
		}
	}
}

// 2. 바닐라 Detour 규격의 Multi-Tile 네비메시 파일 로더 (NavMeshLoader.cpp)
dtNavMesh* NavMeshLoader::LoadNavMeshFromBin(const char* path)
{
    std::FILE* fp = std::fopen(path, "rb");
    
    // 헤더(Magic, Version) 검증 후 dtNavMesh 초기화
    NavMeshSetHeader header{};
    std::fread(&header, sizeof(NavMeshSetHeader), 1, fp);
    dtNavMesh* mesh = dtAllocNavMesh();
    mesh->init(&header.params);

    // 타일별로 데이터를 읽어들여 할당하고 추가 (DT_ALLOC_PERM 사용)
    for (int i = 0; i < header.numTiles; ++i)
    {
        NavMeshTileHeader tileHeader{};
        std::fread(&tileHeader, sizeof(tileHeader), 1, fp);
        
        unsigned char* data = (unsigned char*)dtAlloc(tileHeader.dataSize, DT_ALLOC_PERM);
        std::fread(data, tileHeader.dataSize, 1, fp);
        mesh->addTile(data, tileHeader.dataSize, DT_TILE_FREE_DATA, tileHeader.tileRef, nullptr);
    }
    std::fclose(fp);
    return mesh;
}

// 3. 플레이어 이동 위치 서버 보간 (Player.cpp / Field.cpp)
void FieldInstance::UpdatePlayerPosition()
{
	uint64 now = GetTickCount64();
	for(auto& player : _players)
	{
		if(player->IsMoving())
		{
			// 경과 시간에 따른 현재 이동 비율(Lerp)을 계산하여 서버 내부 좌표 동기화
			player->Transform()->SetPos(player->GetCurrentPosition(now));
		}
	}
	
	// 500ms 마다 갱신 타이머 예약
	JobRef job = make_shared<Job>([weakSelf = weak_from_this()]() { /* UpdatePlayerPosition */ });
	LJobTimer.Reserve(500, GetJobQueue(), job);
}
```