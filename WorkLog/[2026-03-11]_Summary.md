# 2026-03-11 작업 요약

## 1. 게임 오브젝트 ID 시스템 구축 및 캡슐화 강화
- 기존의 `GameObjectIDUtil` 네임스페이스를 제거하고, 모든 ID 생성 및 분석 로직을 `GameObject` 클래스 내부로 직접 통합했습니다.
- ID 분석 함수(`GetType`, `GetTag`, `GetInstanceID` 등)를 `static`에서 인스턴스 멤버 함수로 개선하여, `obj->GetType()`과 같이 객체에서 직접 정보를 조회할 수 있도록 사용성을 높였습니다.
- ID 생성 핵심 로직(`MakeID`)은 `private static`으로 설정하여 객체 지향적인 캡슐화를 강화했습니다.
- 네트워크 확장성 및 64비트 ID 체계 지원을 위해 `ObjectInfo`의 ID 타입을 `int32`에서 `uint64`로 변경하고 관련 Protobuf 명세 및 생성된 코드를 모두 갱신했습니다.

## 2. 로그 시스템 고도화 및 인프라 안정성 확보
- 로그 출력 시 시스템별 카테고리(`Default`, `PathFind`, `NavMesh`, `Thread` 등)를 지정할 수 있는 기능을 도입하여 시스템 규모가 커짐에 따라 발생하는 로그 혼선을 줄이고 가독성을 획기적으로 개선했습니다.
- `AsyncActor`의 소멸자를 `virtual`로 변경하여 상속 구조에서 객체가 안전하게 파괴되도록 보장했습니다.
- `Field` 객체 소멸 시 Detour 라이브러리의 `dtNavMeshQuery` 자원을 명시적으로 해제하는 로직을 추가하여 메모리 누수 가능성을 차단했습니다.

## 3. 실시간 이동 상태 동기화 및 입장 로직 개선
- 플레이어가 필드 입장 시, 이미 필드 내에서 이동 중인 주변 캐릭터들의 최신 이동 경로(`Waypoints`)와 이동 시작 서버 틱을 즉시 전송하도록 로직을 개선했습니다.
- 이를 통해 클라이언트가 중도 입장하더라도 주변 캐릭터들이 정지해 있다가 갑자기 튀어 나가는 현상을 방지하고, 서버 틱 기반의 정밀한 보간 이동이 가능해졌습니다.
- `Field::EnterPlayer` 비동기 작업 내에서 세션 및 플레이어 유효성 검사 로직을 강화하여 네트워크 예외 상황에 대비했습니다.


[코드 변경 사항 요약]
```cpp
// 1. GameObject 내부로 통합된 ID 시스템 (GameObject.h)
class GameObject : public enable_shared_from_this<GameObject>
{
public:
    // 객체의 ID를 기반으로 정보를 즉시 분석 (인스턴스 멤버 함수로 개선)
    uint16 GetTag() { return static_cast<uint16>(GetId() >> 48); }
    EObjectType GetType() { return static_cast<EObjectType>(GetId() >> 60); }
    uint64 GetInstanceID() { return (GetId() & 0x0000FFFFFFFFFFFFULL); }

protected:
    // 자식 클래스에서 태그 생성 시 사용 (Protected)
    static uint16 MakeTag(EObjectType type, uint16 subId) { 
        return (static_cast<uint16>(type) << 12) | (subId & 0x0FFF); 
    }
    void SetId(uint16 tag) { _id = MakeID(tag, _instanceIdGenerator.fetch_add(1)); }

private:
    // 내부 핵심 생성 로직 (Private)
    static uint64 MakeID(uint16 tag, uint64 instanceID) { 
        return (static_cast<uint64>(tag) << 48) | (instanceID & 0x0000FFFFFFFFFFFFULL); 
    }
};

// 사용 예시 (Player.cpp)
LOG_DEBUG(Default, "Player[{}] Created", GetInstanceID()); // GetInstanceID(GetId()) 대신 사용

// 2. 카테고리 기반 로그 시스템 (LogManager.h)
#define LOG_INFO(Category, Format, ...) \
    LogManager::Instance().WriteLog(ELogLevel::Info, #Category, std::format(Format __VA_OPT__(,) __VA_ARGS__))

// 3. 입장 시 주변 캐릭터 이동 상태 즉시 동기화 (Field.cpp)
// ... 실시간 이동 경로(Waypoints) 패킷 추가 전송 로직 적용 ...
```
